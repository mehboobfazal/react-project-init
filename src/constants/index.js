import * as WorkerConstants from "./workerConstants";
import * as SessionStorageConstants from "./sessionStorage";

const AppConstants = {
    WorkerConstants,
    SessionStorageConstants,
};
export default AppConstants;
